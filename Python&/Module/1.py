# 내장 모듈 / 모듈은 어떠한 부품이라고 생각하면 됨
# 연관되어 있는 함수들의 그룹
import math

# ceil 2보다 더 큰 정수를 리턴해 준다
print(math.ceil(2.2))
# floor는 현재 숫자에서 가장 작은 정수 즉 소수점 을 없앤다
print(math.floor(2.9))
# ()의 숫자에 루트를 씌워 제곱되기 전의 숫자를 구한다.
print(math.sqrt(4))

